import React from 'react';

const Setting = (props) => {
    return (
        <div className='setting-content'>
            This is a setting component
        </div>
    );
};

export default Setting;